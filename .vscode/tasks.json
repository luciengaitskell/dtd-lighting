{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "circuitpy",
            "type": "shell",
            "command": "rsync -a --prune-empty-dirs --include '*/' --include '*.py' --exclude '*' . /Volumes/CIRCUITPY",
            "problemMatcher": []
        }
    ]
}
